<% if @feed_items.any? %>
  <ol class="microposts">
    <%= render @feed_items %>
    <!-- Rails will still look for the _micropost partial when rendering @feed_items -->
  </ol>
  <%= will_paginate @feed_items %>
<% end %>